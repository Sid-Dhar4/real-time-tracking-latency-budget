# Minimal runtime dependencies for the tracking-latency benchmark.
# Install with:
#   python -m pip install -r environment/requirements-minimal.txt

numpy
pandas
scipy
matplotlib
opencv-python
pyyaml
tqdm
ultralytics
lap
